spring:
  application:
    name: schedule-management-app

  # 데이터베이스 연결 정보
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/scheduler
    username: root
    password: root

# Swagger 에서 사용할 OpenAPI 스펙 문서 위치 설정
springdoc:
  swagger-ui:
    url: /docs/open-api-3.0.1.json
